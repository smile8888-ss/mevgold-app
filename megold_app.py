import firebase_admin
from firebase_admin import credentials, messaging

# ==========================================
# 1. ตั้งค่า Firebase (ใช้ไฟล์กุญแจ JSON)
# ==========================================
try:
    # อ้างอิงถึงไฟล์กุญแจที่เราอัปโหลดไว้
    # (ต้องชื่อตรงกับไฟล์ที่คุณอัปโหลดเป๊ะๆ นะครับ)
    cred = credentials.Certificate("firebase_key.json")
    
    # เช็คเพื่อไม่ให้ Error ถ้ามีการรันซ้ำ
    if not firebase_admin._apps:
        firebase_admin.initialize_app(cred)
    print("✅ เชื่อมต่อ Firebase สำเร็จ!")

except Exception as e:
    print("❌ เชื่อมต่อ Firebase ไม่ได้ (เช็คชื่อไฟล์ JSON):", e)

# ==========================================
# 2. ฟังก์ชันส่งแจ้งเตือน (FCM ยิงตรง)
# ==========================================
def send_push_to_app(buy_price, sell_price):
    print(f"กำลังเตรียมส่งข้อมูล: {buy_price} / {sell_price}")
    
    # สร้างข้อความ
    message = messaging.Message(
        notification=messaging.Notification(
            title="🔔 ราคาทองเปลี่ยน!",
            body=f"รับซื้อ: {buy_price} | ขายออก: {sell_price}",
        ),
        # ส่งไปที่หัวข้อ 'all' (เดี๋ยวเราไปตั้งค่ารับในแอป)
        topic="all",
    )

    # ยิงออกไป
    try:
        response = messaging.send(message)
        print("✅ ส่งแจ้งเตือนสำเร็จ! Message ID:", response)
    except Exception as e:
        print("❌ ส่งไม่ผ่าน:", e)

# ==========================================
# 3. ส่วนดึงราคา (Force Test Mode)
# ==========================================
def check_gold_price():
    print("--- เริ่มต้น Force Test (Firebase) ---")
    
    # จำลองราคาเพื่อทดสอบ
    test_buy = "45,000"
    test_sell = "45,100"
    
    # สั่งส่งทันที
    send_push_to_app(test_buy, test_sell)

if __name__ == "__main__":
    check_gold_price()
