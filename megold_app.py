import requests
import json
from bs4 import BeautifulSoup

# =====================================================
# 🔑 ส่วนตั้งค่า (แก้แค่ 2 บรรทัดนี้)
# =====================================================
ONESIGNAL_APP_ID = "f2770b48-2c38-4b73-9ba8-6c11469fe53e" 
# เปลี่ยน Basic เป็น Bearer นะครับ
ONESIGNAL_API_KEY = "Bearer os_v2_app_6j3qwsbmhbfxhg5inqiunh7fhzbe4ctsbnlu56ny4m4s7yje2yvy233orfxnlgfop4lw23elugqcnwu7qwh5iwmrflb2kxbntmj5tcy"

# =====================================================
# 📨 ฟังก์ชันส่งแจ้งเตือน (ยิงเข้ามือถือ)
# =====================================================
def send_push_to_app(buy_price, sell_price):
    header = {
        "Content-Type": "application/json; charset=utf-8",
        "Authorization": ONESIGNAL_API_KEY
    }
    
    # ข้อความแจ้งเตือน
    payload = {
        "app_id": ONESIGNAL_APP_ID,
        "included_segments": ["All"], # ส่งให้ทุกคนที่ลงแอป
        "headings": {"en": "🔔 ทดสอบระบบแจ้งเตือน"},
        "contents": {"en": f"ระบบทำงานปกติ! ราคาปัจจุบัน\nรับซื้อ: {buy_price}\nขายออก: {sell_price}"}
    }
    
    try:
        print(f"กำลังส่งแจ้งเตือนไปยัง App ID: {ONESIGNAL_APP_ID}...")
        req = requests.post("https://onesignal.com/api/v1/notifications", headers=header, json=payload)
        print("✅ สถานะการส่ง:", req.status_code)
        print("📄 ผลลัพธ์จาก Server:", req.text)
    except Exception as e:
        print("❌ ส่งไม่ผ่าน Error:", e)

# =====================================================
# 🕵️‍♂️ ฟังก์ชันดึงราคาทอง (Scraper)
# =====================================================
def check_gold_price():
    print("--- เริ่มต้นการทำงาน ---")
    
    url = "https://www.goldtraders.or.th/"
    try:
        response = requests.get(url, timeout=10)
        response.encoding = "utf-8"
        soup = BeautifulSoup(response.text, 'html.parser')
        
        # หาจุดที่มีราคาทอง (ส่วนนี้อาจต้องปรับถ้าเว็บเปลี่ยน)
        # แต่เพื่อการทดสอบ ผมจะดึงตัวแรกที่เจอ
        price_elements = soup.find_all(string=lambda text: text and "ทองคำแท่ง" in text)
        
        # --- ⚠️ โหมดบังคับทดสอบ (Force Test) ---
        # สมมติว่าดึงราคามาได้ (หรือใช้ราคาจำลองเพื่อทดสอบการส่ง)
        test_buy = "43,000"  
        test_sell = "43,100"
        
        # พยายามดึงราคาจริง (ถ้าดึงได้จะมาทับค่าข้างบน)
        try:
             # Logic อย่างง่าย: หา span ที่มีราคา (ปรับตามเว็บจริงของคุณได้)
             # ตรงนี้ผมใส่ค่าหลอกไว้ก่อน เพื่อให้แน่ใจว่าแจ้งเตือนจะเด้ง 100%
             pass 
        except:
            print("ดึงราคาจริงไม่ได้ ใช้ราคาจำลองแทน")

        print(f"ราคาที่จะส่ง: ซื้อ {test_buy} / ขาย {test_sell}")
        
        # 🔥 [จุดสำคัญ] สั่งส่งทันที ไม่ต้องเช็ค if ว่าราคาเปลี่ยนไหม
        send_push_to_app(test_buy, test_sell)
        
    except Exception as e:
        print("❌ เกิดข้อผิดพลาดในการดึงข้อมูล:", e)
        # แม้เว็บล่ม ก็ยังจะส่งแจ้งเตือน Error ไปบอก (เพื่อเทสระบบ)
        send_push_to_app("Error", "เว็บเข้าไม่ได้")

if __name__ == "__main__":
    check_gold_price()
